version: "3.8"
services:
  app:
    image: "jtoper/python-webserver:latest"
    ports:
      - "80:80"
    volumes:
      - .:/app

networks:
  default:
    external: true
    name: phpmyadmin_default